framework: fastapi

runtime:
  docker: Dockerfile
  startup: docker build -t sample-repo .
  timeout_seconds: 60
  session_timeout_seconds: 600

health:
  endpoint: /health

tests:
  command: pytest

network:
  required: false
